
import React from 'react';
import { Box, Typography } from '@strapi/design-system';

const ExperienceTab = () => {
  return (
    <Box padding={4}>
      <Typography variant="beta">Experience Level Settings</Typography>
      {/* Add experience level configuration fields here */}
    </Box>
  );
};

export default ExperienceTab;
