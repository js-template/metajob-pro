# Railway Environment Variables for Strapi Backend
# Copy these to your Railway service environment variables

# Basic Strapi Configuration
HOST=0.0.0.0
PORT=$PORT
NODE_ENV=production

# Security Keys (Generate new ones for production)
APP_KEYS="$APP_KEYS"
API_TOKEN_SALT="$API_TOKEN_SALT"
ADMIN_JWT_SECRET="$ADMIN_JWT_SECRET"
TRANSFER_TOKEN_SALT="$TRANSFER_TOKEN_SALT"
JWT_SECRET="$JWT_SECRET"

# PostgreSQL Database Configuration (Railway will provide these)
DATABASE_CLIENT=postgres
DATABASE_HOST=$PGHOST
DATABASE_PORT=$PGPORT
DATABASE_NAME=$PGDATABASE
DATABASE_USERNAME=$PGUSER
DATABASE_PASSWORD=$PGPASSWORD
DATABASE_SSL=true

# Cloudinary Configuration (Optional - for file uploads)
CLOUDINARY_NAME="$CLOUDINARY_NAME"
CLOUDINARY_KEY="$CLOUDINARY_KEY"
CLOUDINARY_SECRET="$CLOUDINARY_SECRET"

# Email Provider Configuration (Optional)
EMAIL_PROVIDER="$EMAIL_PROVIDER"
EMAIL_NAME="$EMAIL_NAME"
EMAIL_EMAIL="$EMAIL_EMAIL"
BREVO_API_URL="$BREVO_API_URL"
BREVO_API_KEY="$BREVO_API_KEY"

# NPM Token for private registry
NPM_TOKEN="$NPM_TOKEN"